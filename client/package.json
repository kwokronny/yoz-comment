{
  "name": "kb-comment",
  "version": "1.0.0",
  "description": "open source blog comment system",
  "main": "index.js",
  "scripts": {
    "start": "parcel serve src/client.ts src/index.html src/themes/{light,dark}.styl --out-dir ../templates/static --public-url ./ --port 5000",
    "build": "parcel build src/client.ts src/index.html src/themes/{light,dark}.styl --out-dir ../templates/static --public-url ./ --experimental-scope-hoisting --no-content-hash",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kwokronny",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "stylus": "^0.54.8",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@hiyoz/stylus-shortcut": "^1.1.0"
  }
}
