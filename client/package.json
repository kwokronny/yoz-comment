{
  "name": "kb-comment",
  "version": "1.0.0",
  "description": "open source blog comment system",
  "main": "index.js",
  "scripts": {
    "start": "parcel serve src/index.html src/themes/{light,dark}.styl --port 4000",
    "build": "parcel build src/kb-comment.ts src/themes/{light,dark}.styl --experimental-scope-hoisting --no-content-hash",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "stylus": "^0.54.8",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@hiyoz/stylus-shortcut": "^1.1.0"
  }
}
